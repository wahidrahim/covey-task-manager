<%= form_for :task, url: tasks_add_path do |f| %>
	<%= f.text_field :name, placeholder: 'name' %>
	<%= f.range_field :urgency %>
	<%= f.range_field :importance %>
	<%= f.submit 'add task' %>
<% end %>
